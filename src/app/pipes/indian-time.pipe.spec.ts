import { IndianTimePipe } from './indian-time.pipe';

describe('IndianTimePipe', () => {
  it('create an instance', () => {
    const pipe = new IndianTimePipe();
    expect(pipe).toBeTruthy();
  });
});
